spring.application.name=FundInsightBackend
server.port=8080

# MySQL Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/fundinsightdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Srikar9963}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# JWT Configuration
application.security.jwt.secret-key=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
application.security.jwt.expiration=86400000

# ================================================================
# EMAIL PROVIDER CONFIGURATION
# ================================================================
# Currently using: GMAIL SMTP (Active)
# Users can register with ANY email - Gmail, Yahoo, Outlook, etc.
# The credentials below are only for the SENDER account.
# ================================================================

# -------------------------------------------------------
# OPTION A: Gmail SMTP (Currently Active - Free, 500/day)
# -------------------------------------------------------
# Works for all recipient emails. May occasionally land in spam
# for non-Gmail users. No extra signup needed.
# -------------------------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chintuN4447@gmail.com
spring.mail.password=npfuqloljjtteajx

# -------------------------------------------------------
# OPTION B: Brevo SMTP (Best Free Option - 300 emails/day)
# Better inbox delivery for Yahoo, Outlook, company emails.
# 1. Sign up FREE (no credit card): https://app.brevo.com
# 2. Go to: Account -> SMTP & API -> SMTP tab
# 3. Copy Login and Master Password
# 4. Comment out OPTION A above and uncomment below:
# -------------------------------------------------------
#spring.mail.host=smtp-relay.brevo.com
#spring.mail.port=587
#spring.mail.username=YOUR_BREVO_LOGIN_EMAIL
#spring.mail.password=YOUR_BREVO_SMTP_PASSWORD

# -------------------------------------------------------
# OPTION C: Outlook/Hotmail SMTP (Free, works for all)
# Use if you have a Microsoft account:
# -------------------------------------------------------
#spring.mail.host=smtp-mail.outlook.com
#spring.mail.port=587
#spring.mail.username=YOUR_OUTLOOK_EMAIL@outlook.com
#spring.mail.password=YOUR_OUTLOOK_PASSWORD

# ----------------------------------------------------------------
# Common SMTP Settings (same for all providers above)
# ----------------------------------------------------------------
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# App email display name (shown in recipient's inbox)
app.mail.from-name=FundInsight Pro
